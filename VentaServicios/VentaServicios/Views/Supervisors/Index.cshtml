@model IEnumerable<VentaServicios.ModeloDato.Supervisor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Gerente.IdGerente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuario.TokenContrasena)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Gerente.IdGerente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuario.TokenContrasena)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdSupervisor }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdSupervisor }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdSupervisor })
        </td>
    </tr>
}

</table>
