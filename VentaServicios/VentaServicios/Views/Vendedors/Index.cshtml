@model IEnumerable<VentaServicios.ModeloDato.Vendedor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AspNetUsers.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Supervisor.IdUsuario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TiempoSeguimiento)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AspNetUsers.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Supervisor.IdUsuario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TiempoSeguimiento)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdVendedor }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdVendedor }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdVendedor })
        </td>
    </tr>
}

</table>
